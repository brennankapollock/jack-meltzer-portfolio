/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;

module.exports = {
  basePath: "/jack-meltzer-portfolio", // Replace with your GitHub repository name
};

module.exports = {
  images: {
    unoptimized: true,
  },
  // other configurations...
};
